# 评测机（生成用例、GPU、解析 OMC/API 日志）共用依赖。
# 不包含 vLLM。vLLM 安装见 README。
# 鸿蒙 PC 上的 Machine_test/run_chat.sh 只需要 curl，不要装这些包，也不要 python。
#
# transformers 仅 1_Data_gen（按 chat template 计 token 长度）和 3_OMC_eval（日志缺 token 数时兜底）需要。
# Summary 任务的语义相似度可选：装 sentence-transformers 并在评测脚本里指定 --embedding_model。

openai>=1.30.0
pandas>=2.0.0
pyarrow>=12.0.0
matplotlib>=3.7.0
numpy>=1.24.0
tabulate>=0.9.0
openpyxl>=3.1.0
transformers>=4.51.0
jieba
rouge
chardet
