我想参照Ceval_ref做一份GPU、OMC和API的，基于LongBench-Pro的模型精度评估。目前主要是针对qwen3.5 4B、9B做适配评估，未来或许会加入更多的新模型。对于qwen3.5的多模态评估，位于Multi_model_img_eval的代码里的配置你可以参考。我已经在Long_context_eval/Data_set/LongBench-Pro目录下下载好了数据集。我的测试用例生成需要支持配置最大的用例长度。例如，假如我的kv cache最大为32K，那我就打算配置用例最大长度为27000，大于这个长度的用例就不要，因为输出的最大长度我一般配为5000，这样测试的时候kv cache就不会超过32K。我的代码也是要分为1_Data_gen，2_GPU_run_eval，3_OMC_eval，4_API_eval。目前对于GPU的测试用例，我希望用例类似api那样是role user assistant那样，而不是仅仅是放user prompt的内容。要确保gpu，omc和api评估的一致性，然后依据不同的vertical做break down。模型的配置文件你可以依据需要从其他目录搬移或者帮我写好。所有你帮我完成的代码都位于Long_context_eval。